costmap_node:
  ros__parameters:
    resolution: 0.05       # 5 cm per cell
    size_m: 50.0           # 50 meters square
    inflation_radius: 1    # meters
    update_rate: 20.0      # Timer update frequency in Hz

